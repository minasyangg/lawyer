'use client'

import ServiceDescription, { ServiceDescriptionProps } from './ServiceDescription'

export default function ServiceDescriptionClient(props: ServiceDescriptionProps) {
  return <ServiceDescription {...props} />
}